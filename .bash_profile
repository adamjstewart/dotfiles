#!/usr/bin/env bash

# .bash_profile

# Sourced when bash is invoked as an interactive login shell (e.g., ssh)
# On macOS, Terminal.app automatically opens new shells as login shells

# Get user aliases, settings, and functions
if [[ -f ~/.bashrc ]]; then
    source ~/.bashrc
fi

